import Login from '@/components/login';
import React from 'react';

const LoginPage = () => {
  return (
    <main className="flex bg-green-600 justify-center items-center h-full">
      <Login />
    </main>
  );
};

export default LoginPage;
